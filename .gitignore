node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

.env
.env.local
.env*.local

playwright-report/
test-results/
.playwright/

# Persönliche Claude-Code-Einstellungen. Die geteilten stehen in
# .claude/settings.json und gehören ins Repository.
.claude/settings.local.json

.DS_Store
*.log

# /design-sync (claude.ai/design): staged converter scripts + isolated deps,
# build output, and generated/verification state under .design-sync/. The
# durable set (config.json, NOTES.md, synth-src/, conventions.md, previews/,
# overrides/) is committed — see .design-sync/NOTES.md.
/.ds-sync/
/ds-bundle/
/.design-sync/.cache/
/.design-sync/learnings/
/.design-sync/node_modules
